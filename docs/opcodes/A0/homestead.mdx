## Gas

    static_gas = {gasPrices|log}
    dynamic_gas = {gasPrices|logTopic} * topic_count + {gasPrices|logData} * size + memory_expansion_cost

对于 LOG0，“topic_count”为 0。
内存扩展成本说明可以在[此处](/about)找到。
