---
fork: 坎昆
group: 区块信息
---

*索引 1 位于栈顶。参见 [PUSH](/#60)。*

## 堆栈输入

0. `index`

## 堆栈输出

0. 如果 `index < len(tx.blob_versioned_hashes)`，则使用 `tx.blob_versioned_hashes[index]`，否则使用零 `bytes32` 值。

## 示例

| * | Input | Output |
|--:|------:|-------:|
| `1` | | `0` |

[在 playground 中复现](/playground?unit=Wei&codeType=Mnemonic&code='BLOBHASH'_)。

## 错误情况

在以下情况下，当前上下文所做的状态更改会被[reverted](#FD)：
- Gas 不足。
- 堆栈上的值不足。
