---
fork: 边境
group: Push 操作
---

*索引 1 位于栈顶。*

## 注意事项

新值会被压入栈顶，所有其他值的索引都会递增。因此，特定操作码对应的值必须按栈顶的逆序压入栈顶。例如，对于 [MSTORE](/#52) 操作码，第一个压入的值必须是 `value`，然后是 `offset`。

## 堆栈输出

0. `value`: 推送值，右对齐（放入最低有效字节）。

## 示例

| * | * |
|---|---|
| **Code** | `0x60FF6000` |
| **Text** | `PUSH1 FF PUSH1 00` |

| * | Input | Output |
|--:|------:|-------:|
| `1` | | `0x00` |
| `2` | | `0xFF` |

[在 playground 中复现](/playground?unit=Wei&codeType=Mnemonic&code='~xFF%5Cn~'~PUSH1%200%01~_)。

## 错误情况

在以下情况下，当前上下文所做的状态更改会被[reverted](#FD)：
- Gas 不足。
- Stack overflow.
